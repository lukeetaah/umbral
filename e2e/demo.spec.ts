import { expect,test } from "@playwright/test";
test("home explains the claim and limits",async({page})=>{await page.goto("/");await expect(page.getByRole("heading",{name:/Puede un sonido/})).toBeVisible();await expect(page.getByText("No lee tu cerebro.")).toBeVisible()});
test("demo is usable without Supabase",async({page})=>{await page.goto("/demo");await expect(page.getByText("MODO LOCAL")).toBeVisible();await page.getByRole("checkbox").check();const start=page.getByRole("button",{name:/Comenzar calibración/});await expect(start).toBeEnabled();await start.click();await expect(page.getByText("Volumen bajo y cómodo")).toBeVisible();await expect(page.getByRole("button",{name:"Detener todo el audio"})).toBeVisible()});
