"use client";
import { useState } from "react";
import { getSupabaseClient } from "@/src/lib/supabase/client";
export function AccountPanel(){const [email,setEmail]=useState("");const [message,setMessage]=useState("");const signIn=async()=>{const supabase=getSupabaseClient();if(!supabase){setMessage("Supabase no está configurado. Podés seguir usando el modo local.");return;}const {error}=await supabase.auth.signInWithOtp({email,options:{emailRedirectTo:window.location.origin+"/account"}});setMessage(error?error.message:"Revisá tu correo para continuar. No se sincronizó ningún dato.");};return <section className="account-card"><label>Correo electrónico<input type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email"/></label><button className="button primary" onClick={signIn} disabled={!email.includes("@")} type="button">Enviar enlace de acceso</button><p>{message}</p><small>La cuenta no implica consentimiento de investigación. La sincronización se inicia por separado desde el demo.</small></section>}
